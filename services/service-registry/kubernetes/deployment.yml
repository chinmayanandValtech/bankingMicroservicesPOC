apiVersion: apps/v1
kind: Deployment

metadata:
  name: service-registry

spec:
  replicas: 1

  selector:
    matchLabels:
      app: service-registry

  template:
    metadata:
      labels:
        app: service-registry

    spec:
      containers:
        - name: service-registry
          image: chinmayanand5613/service-registry:1.1
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8761