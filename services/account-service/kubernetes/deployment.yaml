apiVersion: apps/v1
kind: Deployment

metadata:
  name: account-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: account-service

  template:
    metadata:
      labels:
        app: account-service

    spec:
      containers:
        - name: account-service

          image: chinmayanand5613/account-service:1.1
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8082

          env:
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/accountdb

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD

            - name: CONFIG_SERVER_URL
              value: http://config-server:8888

            - name: EUREKA_URL
              value: http://service-registry:8761/eureka

            - name: SPRING_PROFILES_ACTIVE
              value: docker

            - name: ZIPKIN_ENDPOINT
              value: http://zipkin:9411/api/v2/spans