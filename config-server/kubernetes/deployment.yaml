apiVersion: apps/v1
kind: Deployment

metadata:
  name: config-server

spec:
  replicas: 1

  selector:
    matchLabels:
      app: config-server

  template:
    metadata:
      labels:
        app: config-server

    spec:
      containers:
        - name: config-server
          image: snehasatish/config-server:v1
          imagePullPolicy: Always

          ports:
            - containerPort: 8888

          env:
            - name: EUREKA_URL
              value: "http://service-registry:8761/eureka"